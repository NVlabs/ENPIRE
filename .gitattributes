third_party/bundlesdf/libs/*.so* filter=lfs diff=lfs merge=lfs -text
third_party/bundlesdf/libs/libBundleTrack.so filter=lfs diff=lfs merge=lfs -text
# Locally-rebuilt BundleSDF runtime closure (produced by install/compile_bundlesdf.sh).
# Built against glibc-2.17 sysroot so it runs on any modern Linux + Python 3.11 + CUDA 12.x.
third_party/bundlesdf_5090/*.so* filter=lfs diff=lfs merge=lfs -text
# AnyGrasp checkpoints, per-machine licence archives, and the compiled SDK are
# supplied by each operator and must never be committed — see SECURITY.md and
# enpire/env/docs/ANYGRASP_SETUP.md. They are covered by .gitignore rather than
# LFS filters, because an LFS rule here would imply committing them is expected.
experimental/pico/XRoboToolkit_PC_Service_1.0.0_ubuntu_22.04_amd64.deb filter=lfs diff=lfs merge=lfs -text
