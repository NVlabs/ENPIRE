# Hello Environment

> **What:** deterministic code-as-policy loop · **Needs:** base install · **Time:** under 5 seconds

Run it with:

```bash
uv run enpire examples run 00_hello_environment
```

The run writes `result.json` and `events.jsonl` under
`outputs/hello-environment/`.
